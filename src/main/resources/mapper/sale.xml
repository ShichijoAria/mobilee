<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.xhu.mobilee.dao.SaleDao">

    <select id="selectSaleByMobilePhoneId" parameterType="long" resultType="HashMap">
        SELECT sa.id,sa.name,sa.address,se.price FROM t_sale sa JOIN t_sell se ON se.sale=sa.id JOIN t_mobile_phone mo ON mo.id=se.mobile_phone AND mo.id=#{id} LIMIT 0,10;
    </select>

    <select id="findSaleById" parameterType="long" resultMap="saleResult">
        SELECT
        s.id  AS s_id,
        s.name  AS  s_name,
        s.address AS s_address,
        s.created AS s_created,
        s.edition AS s_edition,
        a.id AS a_id,
        a.name AS a_name
        from t_sale s join t_admin a on s.author = a.id
        where s.id = #{id}
    </select>

    <update id="updateSaleById" parameterType="SaleEntity">
        UPDATE t_sale
        <set>
            <if test="name != null">name=#{name},</if>
            <if test="address != null">address=#{password},</if>
            <if test="author != null">author=#{author},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteSale" parameterType="list">
        DELETE FROM t_sale WHERE id IN
        <foreach collection="list" item="ids"  open="(" separator="," close=")">
            #{ids}
        </foreach>
    </delete>

    <insert id="insertSale" parameterType="SaleEntity" keyProperty="id">
        INSERT INTO t_sale (name, address, author,created) VALUES (#{name},#{address},#{author.id},current_timestamp)
    </insert>

    <resultMap type="SaleEntity" id="saleResult">
        <id property="id" column="s_id" />
        <result property="name" column="s_name" />
        <result property="address" column="s_address" />
        <association property="author" resultMap="authorResult"/>
    </resultMap>

    <resultMap id="authorResult" type="AdminEntity">
        <id property="id" column="a_id" />
        <result property="name" column="a_name" />
        <result property="password" column="a_password" />
    </resultMap>

</mapper>